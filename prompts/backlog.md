# Ballpark prompts backlog

Design and styling prompts for Claude Code, with their visual references. This file is the source of truth for what's done, what's in flight, and what's still in the pile — pick from it as needed, add to it as new work surfaces.

## How this works

Each piece of design work lives here as an indexed **pair**:

- `pNNNN-<slug>-prompt.md` — the written spec: what to change, which files, which tokens, how to verify.
- `pNNNN-<slug>-mockup.html` — a standalone visual reference. Self-contained; opens in any browser.

The `pNNNN` prefix (zero-padded, e.g. `p0001`) gives each task a stable ID and keeps the folder sorted in order. Both files in a pair share the same prefix and slug.

When picking up a task, read the `-prompt.md` and open the matching `-mockup.html` so you have both the spec and the picture.

## Conventions

- Prompts reference real file paths and the v1.22 elevation tokens in `client-angular/src/styles.css`. Use existing tokens — never hardcode shadows, radii, or hex colours, never invent new tokens.
- Prompts are styling/layout scoped unless they say otherwise — don't change data flow or business logic.
- Theme colours (`--theme-*`) recolour with the admin preset; semantic colours (`--color-action/waiting/quoted/booked`, success/danger) do not.
- Icons are Lucide only, via `lucide-angular` — never Tabler or any other set.
- **Never edit a prompt once written.** To change or fix something, create a new numbered prompt that supersedes it. A prompt always means one fixed thing.
- A prompt usually pairs with its own `-mockup.html`, but a follow-up/fix prompt may instead reference an earlier mockup as its target (note that in the prompt and the Index row).

## Index

| ID | Status | Work |
|----|--------|------|
| p0001 | Done | Search-panel standardisation across Marketplace + Messages, and the Messages styling pass (panel containment, sticky headers, shared list-row card, elevation-token migration). |
| p0002 | Done | Styling fixes after p0001's first pass — white search panel, stray container removed, panel-header divider alignment, header icons de-circled, active category circle. Prompt only; uses the p0001 mockup as its target. |
| p0003 | Done | Bold hero mode + real tabs — new `[data-mode="bold"]` (themed accent hero with code-generated orbs + grain), project tabs restyled as real folder tabs with a light contrast-colour active tab, new `--theme-contrast` token per preset. |
| p0004 | Done | Marketplace round-3 fixes — kill the apparent pink wrapper around the cat + search panels (browse-strip horizontal inset / gutter issue), and remove the ghost hairline at the bottom of the category-circles panel. Prompt only; uses the p0001 mockup as its target. |
| p0005 | Done | Remove the Plan tab without regression — AI matching and per-category brief editing are already wired through the Marketplace, so this is navigation cleanup only: remove the tabs entry, redirect `/plan` + `/brief` to `/marketplace`, repoint the Overview BRIEF card, delete `tabs/brief/`. Prompt only. |
| p0006 | Done | Inbox simplification — reply-only surface (email-launching belongs to the cart). New list-mode thread card (logo + supplier + bold-when-unread subject + status badge + read dot), same treatment for card and table views, calm empty state pointing back to the cart. |
| p0007 | Ready | Marketplace finalize — drop the ✉ envelope from item cards (email lives in the cart now), confirm panel headers stay calm (no bold-fill experiment shipped), verify per-category brief editing in the marketplace context panel post-Plan-removal, sweep all five theme presets in Light + Bold for contrast issues, ensure inbox category circles + quick actions match the marketplace. Prompt only. |
| p0008 | Done | Supplier consumer view + inbox conversation pattern — chat-style conversation panel (items inline as cards + summary index at top + chat bubbles below + compose at foot), per-item action buttons (Accept / Decline / Adjust → Pay / Decline / Adjust post-agreement), 9-status codelist with transition table (incl. Holding), decline-reason codelists (pre + post), quick-reply chips, universal clock primitive for `next_action_by` (drives due/overdue), public `/brief/:token` route, HTML notification email template, light-structured reply endpoint, inbox refinements (contact name, ref-subject, date/time), enable outreach emails. Polish TODOs in-code: §4.1 collapsible summary header, §4.3 quick-reply chips + clock popover + paperclip, §4.2 chat-bubble alignment polish + grain pattern. |
| p0009 | Superseded | Inbox item card → marketplace shape. Rolled forward through p0010, now p0011. Don't implement directly. |
| p0010 | Superseded | Conversation panel refinements v1 — header approach changed. Rolled forward into p0011. Don't implement directly. |
| p0011 | Done | Conversation panel refinements (v2) — supersedes p0009, p0010, and p0008 §4.4. Same three jobs: (1) item card adopts marketplace shape with three deltas (action slot / status pill / was→now price); (2) symmetric action table — `Think · Decline · Adjust` universal at any non-terminal state, `Accept` turn-based, `Pay` agent-only on `accepted`; Think is `holding` with a friendlier button label that opens the clock popover; (3) conversation header reuses the existing **supplier list-mode card** from the catalogue (image + name + `{contact} · {category}` eyebrow), with the thread-level status pill where the heart sits + clock chip when set. Drop the four-pill filter row entirely. TODO(p0011-§1-extract) + TODO(p0011-§3-extract) markers in-code for the shared SCSS / standalone `SupplierRowCardComponent` extraction follow-up. |
| p0012 | Ready | Data hygiene — three small fixes: (1) drop the legacy `WA-016 — Brief:` projectRef prefix from outreach compose default subjects (fix forward only, no backfill); (2) unique indexes on `items (org_id, lower(trim(name))) WHERE is_active` and `message_items (message_id, item_id) WHERE item_id IS NOT NULL`, with `ON CONFLICT DO NOTHING` audited on insert paths; (3) new `items.display_ref` column generated as `{SupplierInitials}-{seq}` per supplier (mirrors brief `ref_code`), backfilled in creation order, rendered as a muted chip after the supplier eyebrow on both marketplace item cards and `MessageItemCardComponent`. |
| p0013 | Mostly Done | Brief as cards — structured cards carry data, short notes carry warmth (Amazon/Etsy/WhatsApp model). Email-style metadata-table conversation header (From/To/CC/Subject/First/Last/Status — final shape diverged from the §0 single-strip spec; superseded by what shipped). Conversation panel body splits into three collapsible sections: Event details / Items / Conversation. New event-details card. Every state change lands as a structured event card in the stream sender-aligned (them left / me right, max 75% lane width). In-stream item cards use the marketplace grid layout; items section above keeps the row layout. Compose collapsed behind "Add note" — quick-reply chips + item-tag picker + clock are the default. Cold/warm note templates auto-prefill based on prior thread history. **Deferred:** §6 email templates (still prose — held for customer feedback before redesign), §8 outreach compose modal slim-down. Supersedes p0008 §4.2 + §7 and p0011 §3. |
| p0014 | Done | Agent Home (dashboard) restyled with marketplace chrome — parchment background, white floating panels with `--radius-card` + `--shadow-xs`, calm panel headers with Lucide icons. Two new top-level tabs: Home / Settings (calm underline style). Same content as before (stats / Upcoming / Recent Activity / Quick Actions / Active Events grid / Credits / Saved Suppliers). `+ New Event` is a themed-accent filled pill button. Inactive + Past Events sections collapsed by default. |
| p0015 | Done | Single shared Inbox + supplier persona surface — one Angular component drives both `/messages` (agency) and `/inbox` (supplier), switched via a `viewer: 'agency' \| 'supplier'` prop. **(Audit note: the bulk shipped piecemeal across the v1.65 persona/inbox series — backlog was just stale. v1.66l closed it out: consolidated to one canonical `/inbox` route + viewer-aware `InboxComponent` (viewer by persona), `/messages` now redirects to `/inbox`, and the dashboard's embedded-inbox tab was dropped in favour of routing to `/inbox`.)** Strip the agency inbox's heavier chrome (search bar / view-mode toggles / sort dropdown) in favour of the calm chip filter rail + thread row + email-style conversation panel that shipped via p0006 / p0011 / p0013. New supplier surface mounts the same component with `viewer='supplier'`, showing agency names as "From". Persona switcher in the SM avatar dropdown with three identities (SM Sarah Mitchell · Agency Admin / BP Beth Pizey · Ballpark Admin / RF Rocket Food · Supplier) — swaps top-nav set + avatar initials + lands on that persona's Home. Dev-only flag gates the switcher. Front tab = renamed from "Home" on supplier pages (the existing shopfront component). Mockup: `p0015-supplier-inbox-mockup.html`. |
| p0016 | Done | Config strip extraction + ViewChild lifecycle fix — two-part task. **Step 1 (immediate):** fix `dashboard.component.ts` `@ViewChild('cfgStripTpl', { static: true })` → default `static: false`. The template lives inside `*ngIf="activeTab === 'projects'"`; `static: true` is invalid for queries inside structural directives and was the root cause of the settings cog disappearing on home after agent shipped (v1.65hC/hD). Grep the codebase for any other `{ static: true }` on ViewChilds targeting templates inside *ngIf / *ngFor / `<ng-template>` and fix those too (marketplace likely). **Step 2 (next commit):** extract `<app-config-strip>` as a shared standalone component owning its own ng-template, handlers, option arrays, `settingsDraft` state, ConfigService subscription, and lifecycle. Delete the duplicate from dashboard and agent. Adds rules to WORKING_STANDARDS: "Extract before duplicate" (30+ lines / 3+ handlers / synced state = mandatory extraction) and "ViewChild `static: true` is forbidden inside structural directives." Shipped v1.65hG/hH/hI: net -121 LOC. Selector landed as `<app-page-config-strip>` to avoid collision with an existing `config-strip` in marketplace. |
| p0017 | Done | Migrate page-config-strip → page-config-drawer — pure container swap, no functional changes. Rename component + folder, wrap body in `<p-sidebar styleClass="bp-drawer" position="right">` per WORKING_STANDARDS drawer pattern, sub-group existing settings under GENERAL (page label / credits / events) / APPEARANCE (theme / align / nav) / SECTIONS (existing components multi-toggle). Top-nav cog toggles drawer visibility instead of strip expansion. Replace `setTemplate(template)` lifecycle pattern with simpler `register(this)` / `unregister()` + `visible$` signal. No Save/Cancel — settings still save on change. Strip-specific horizontal CSS deleted. Sets up the container for the section-visibility checkbox list that comes next. Shipped v1.65hJ. |
| p0018 | Done | Dashboard SECTIONS checkbox toggles — close the loop on p0016/p0017. Five new `ConfigService` flags (`showQuickActions` / `showActiveProjects` / `showCredits` / `showSavedSuppliers` / `showRecentActivity`), all default `true`. Wrap each dashboard body section in `*ngIf` against its flag; columns collapse cleanly when all sections in them are hidden. Replace SECTIONS pill row in the drawer with a vertical `p-checkbox` list, split into HERO sub-group (User name / Location) + SECTIONS sub-group (Upcoming / Stats / Quick Actions / Active {{ projectLabel }}s / {{ creditLabel }}s card / Saved Suppliers / Recent Activity). Labels interpolate the configurable label tokens live. Save on change, no Save/Cancel. Delete the old `componentOptions` / `isComponentActive` / `toggleComponent` plumbing. Drawer now has four top-level groups: GENERAL / APPEARANCE / HERO / SECTIONS. Flat flags — no per-persona storage, single user for now. |
| p0019 | Done | Launcher grid — extract `<app-action-tile>` shared component from agent page; replace home centre column's Active Events panel with a 5-card launcher grid (Add / View {Event}s / Inbox / Marketplace / Profile) using the new tile; mirror the same 5-card set on the agent page so both surfaces share the launcher vocabulary. Lucide icons: folder-plus / folder-open / inbox / store / circle-user. Trim Quick Actions in left column (drop redundant Browse Marketplace, keep Browse Suppliers + Invite Member). Includes cleanup of the now-dead `showActiveProjects` flag from p0018. Does NOT build the `/projects` page — that's separate. |
| p0020 | Deferred — after agent ships | Per-persona dashboard config — architectural lift. ConfigService dashboard flags become persona-keyed (`config.dashboards.{personaId}.show*`) so each persona (agent / supplier / admin) stores its own visibility prefs independently. Persona switcher in the avatar dropdown swaps the active config. `<app-action-tile>` set per persona — agent gets the 5 from p0019, supplier and admin get their own (defined in follow-up prompts). The drawer reads/writes the active persona's config slice. Collapses /agent route into /home (option B from chat) — **settled by p0033 (v1.66n): /agent now redirects to the canonical dashboard at `/` and agent.component.ts is deleted.** Remaining p0020 scope = the per-persona config slices. Prompt to be drafted after the agent-first slice ships (p0023 + p0024). |
| p0021 | Deferred — after agent ships | Supplier persona dashboard set — tile list (likely Add Item / View Catalogue / Inbox / Front / Profile, confirm at draft time) + default section visibility for supplier home view. Built on p0020 infrastructure. Small once p0020 lands. |
| p0022 | Deferred — after agent ships | Admin persona dashboard set — tile list (Ballpark-internal cross-org tools, confirm at draft time) + default section visibility for admin home view. Built on p0020 infrastructure. Small once p0020 lands. |
| p0023 | Done | Hero customisation in the page-config drawer — three small additions, one atomic commit. (1) New Title dropdown in GENERAL group with three options (Org Name / Username / Greeting), default Greeting ("Welcome back, {firstName}"). (2) Rename existing PAGE LABEL field to "Subtitle" (display only — `homePageLabel` data field unchanged). (3) New Hero color segmented control in APPEARANCE group (Theme / None), default Theme — Theme = calm `--theme-soft` wash on hero strip (matches current agent page), None = no tint, hero against `--theme-bg`. Two new ConfigService fields: `heroTitleMode` + `heroColor`. AppShell hero binds to both. Flat flags — per-persona is p0020's lift. |
| p0024 | Done | `/projects` landing page + Projects header button. Two-part: (1) new standalone component at `/projects` with three sections — Active (expanded) / Inactive (collapsed) / Past (collapsed) accordion — reusing the existing project-card grid + styles preserved from pre-p0019. Hero uses ShellContextService so the drawer's Title / Subtitle / Hero color settings apply. Wire `goToProjects()` handlers on dashboard + agent to land here. (2) New `Projects` button in top-nav alongside Home / Admin, Lucide `folder-open` icon, label uses `projectLabel` config token, active state when route is `/projects`. Gate to agent persona if top-nav already differentiates; otherwise add universally and defer per-persona to p0020. Pure reuse — no new components, no project-card redesign, no new ConfigService flags. |
| p0025 | Superseded (shipped then rolled back) | Welcome slide reveal: event-driven → scroll-position-driven. Shipped to dev but exposed a structural seam (per-slide bg colours visible at section boundaries during transition) that needs a rebuild rather than further patching. Rolled forward into p0026 (strip back to static baseline) + p0027 (rebuild with fixed-position bg crossfade, scoped to slide 1 → 2 only to validate the pattern). Don't implement directly. |
| p0026 | Done | Strip welcome to static slides — back out p0025 + the original transition machinery. End state: four slides snap-scroll, content + orbs + grain render, no animated transitions, no bg flood, no scroll-driven anything. Delete `exitingFromSlide`, the bg-flood-first hack, the v1.65hW stage-tall gradient, per-slide `.bp-slide-N` background-color rules, and anything p0025 introduced. Keep marketing content, SVG `feGaussianBlur stdDeviation=20`, grain, scroll-snap, fonts, scroll pill, form. iOS Safari `*ngIf` unmount on SVG temporarily removed — re-added in p0027. Boring but correct baseline for the rebuild. |
| p0027 | Superseded | Welcome bg crossfade — slide 1 → 2 only. Shipped, but architecture (fixed-position bg-stack with opacity crossfade) was the wrong tool for the designer's actual visual effect. The designer's pattern is body-bg colour interpolation + horizontal orb translation, not layered crossfade. Replaced by p0028 (rebuild). Don't extend or iterate p0027 further. |
| p0028 | Superseded | Welcome rebuild — designer's actual pattern (mistaken interpretation). Shipped body-bg interpolation + horizontal orb translation. After inspecting the designer's actual Figma file, the mechanic is fundamentally different: each slide has its own solid bg colour (no interpolation), orbs FADE IN (opacity 0 → 1) as you scroll through a slide (no translation), and orb colour previews the next slide's bg. Also discovered slides 3 + 4 should be GREEN, not blue (`#6391A4` was wrong from v1.65i7). Replaced by p0029. Don't extend p0028 further. |
| p0029 | Superseded (rolled back) | Welcome — orb-grow pattern. Shipped but produced no visible orbs at parked state + flickering orbs during transitions. After three failed attempts (p0027 bg-stack, p0028 body-bg interpolation + horizontal translation, p0029 orb opacity), rolling back to p0026 static baseline via p0030 to reset. Don't extend further. |
| p0030 | Done | Welcome — rollback to p0026 static baseline. Pure file restore: `git checkout 295255b -- client-angular/src/app/public/welcome/welcome.component.ts`. Removes everything from p0027 onwards. Result: four-slides-snap-scroll-with-static-orbs baseline from v1.65iG. No animation, no bg-stack, no JS-driven bg, no orb translation, no orb-opacity. Next transition approach to be decided from this clean ground. |
| p0031 | Done | `/projects` card refresh + layout polish. Four scoped refinements on the page p0024 just shipped: (1) Bigger project cards (`minmax(220px → 320px)`), notification badge pill on cover image top-right (`message-circle` + count + " New", only when action_needed > 0), status pill below title, supplier-count + relative time row, BIG gradient ballpark cost at bottom with `--theme-accent → --color-action-soft` background-clip:text. (2) REF chip above the project name (small `--theme-soft` pill, same treatment as inbox row's `Ref XX-NNN`). (3) Remove "Active Projects" header — section becomes full-width no-border grid. COMPLETED + Past keep their accordion headers (needed for collapse-expand). (4) `+ New {projectLabel}` tile as last item in Active grid only, dashed border, opens `CreateProjectService` modal. No new components (single-page reuse), no model changes, no per-section flags. |
| p0032 | Done | Hero color global + page-config drawer two-tab reorganization. Three changes in one commit: (1) `AppShell.heroIsNone` reads `ConfigService.heroColor` directly (not from `ctx`); drop `ctx.heroColor` plumbing from dashboard + agent; replace with explicit `ctx.useConfiguredTitle: boolean` to preserve "this surface uses configured title (org/user/greeting)" signal. Hero color now globally applied — flip in drawer affects Marketplace, /projects, /inbox, project pages, etc. (2) Drawer body wrapped in `p-tabView` (or `bp-cfg-seg` fallback) with two tabs: Dashboard and General. Default tab on open: Dashboard. (3) Reorganize controls: **Dashboard tab** = Title (org/user/greeting) + Subtitle + Sections checkboxes (Upcoming / Stats / Quick Actions / Credits card / Saved Suppliers / Recent Activity). **General tab** = Theme swatches + Hero color + Hero align + Nav + User name + Location + Credits label + Events label. Drop the four legacy sub-eyebrow groups (GENERAL, APPEARANCE, HERO, SECTIONS) — tabs replace the grouping. All field bindings preserved, save-on-change preserved. Ships after p0033 so it only touches dashboard, not agent (which is gone). |
| p0033 | Done | Role-keyed top-nav + `/agent` consolidation. Three changes in one commit: (1) Collapse `/agent` into `/home` — delete the `/agent` route + agent.component.ts, replace with `redirectTo: 'home'`. The rich `/home` IS the agent dashboard. Settles the "collapse /agent into /home" decision deferred from p0020. (2) Role-keyed top-nav reading from PersonaService — Sarah-as-agent sees `Agent · Inbox · Projects · Marketplace · {orgName}`; Beth-as-platform-admin sees `Config Home · Ballpark`. Supplier nav set deferred to p0021. (3) Org-name as Settings label — rightmost top-nav button binds to `org.name || 'Settings'`, prefers `org.logo_url` avatar over generic icon. Route stays `/settings`; just the label changes. No restyling, no new ConfigService flags, no Config Home admin content (that's a future prompt). |
| p0034 | Done | WORKING_STANDARDS update — page-chrome extraction rule + `<app-update-me>` debt-marker convention. Tiny documentation-only commit. Adds a complementary sub-rule under the existing "Extract Before Duplicate" section to catch page chrome (titles / subtitles / section cards / edit-form scaffolding) — hand-rolled per-page chrome that doesn't trip the markup-copy-paste detector. Names placeholder components (`<app-page-header>`, `<app-section-card>`, `<app-edit-section>`) and the `<app-update-me>` debt-marker banner (introduced in p0035). No code touched. |
| p0035 | Done | `<app-update-me>` banner component + apply to known offenders. Dev-only banner that renders `UPDATE ME — adopt <{{ reason }}> when next refactoring this page` on hand-rolled chrome pages. Never ships to production (`*ngIf="!environment.production"`). Calm parchment + dashed border treatment, non-interactive (no dismiss — only refactor removes it). Apply to Settings/Organisation (page header + two section markers), Settings/Subscription, Settings/Team, Profile (page header + Organisation card section). Greppable inventory via `grep -rn "<app-update-me"`. Doesn't refactor any hand-rolled chrome — that's later prompts. Marks debt, doesn't pay it off. |
| pV2-01f | Done | Brand visual pass: vivid `--bp-gradient` (pink #d63384 → green #16a34a, was soft pastel), new `--bp-text-on-gradient` = white token for text on the gradient, avatar initials switch to white-on-vivid (logo treatment), wordmark vs avatar size parity audit (root cause likely font weight / line-height / font-family inheritance mismatch — fix to bring them to exact visual parity at 16px / weight 600 / line-height 1). Soft pastel still available via existing `--theme-soft`. Out of scope: brand config DB load (that's pV2-01e). Version chip `[Dev v2] v2.02c`. |
| pV2-01e | Done | Brand config from DB. New `bp_brand_config` table (key/value, app-wide) seeded with font_pair / gradient / text_color. New public unauthenticated endpoint `GET /api/brand` returns the map. New client `BrandConfigService` loads at boot before bootstrap, applies values to `:root.style` as `--bp-*` CSS variable overrides. styles.css defaults stay as fallback (no FOUC if API down). Proves the v2 → API → Postgres round-trip end-to-end and sets up runtime-configurable brand pattern. Acceptance includes "change DB row, refresh, font changes" proof. Version chip `[Dev v2] v2.03a`. |
| pV2-01d | Done | Visual tweaks following QC of pV2-01b + pV2-01c. Two changes: (1) `<app-page-hero>` default `accent` flips from `'theme'` to `'none'` — hero band is transparent by default, `accent="theme"` becomes opt-in for routes wanting the wash; (2) `<app-shell>` header drops `border-bottom` — transparent + flush against hero below. `/style/hero` demo rearranges to keep one example with `accent="theme"` for variant QC. Ship report includes a quote of what Liam confirmed working during pV2-01b/01c QC (master tokens via `--bp-*`, font unified between wordmark and avatar initials at 16px, dropdown switching, title/subtitle updates) so it doubles as a record of the QC pass. Version chip `[Dev v2] v2.02b`. |
| pV2-01c | Done | Page Hero (`<app-page-hero>`). Standard hero band component used at top of every feature page in v2 — back link + title + subtitle + accent treatment + align variant + `<ng-content>` slot for right-side actions. Lives in `shell/page-hero/`. Uses `host:`-binding pattern (component IS the band, no inner wrapper). `input.required<string>()` for title. `@let` in templates that consume signals. Hello page updates to use it. New `/style/hero` route shows 4 variants for visual QC. Out of scope: page-settings drawer (the v1 cog overrides) — separate prompt; multiple title modes — defer. Version chip `[Dev v2] v2.02a`. |
| pV2-01a | Done | Codify `host:`-binding component standard in WORKING_STANDARDS. Tiny docs-only commit — adds a rule under the Angular section that v2 components must not wrap their template in a redundant root element; the component instance IS the styled element via `host:` binding for class + variant modifiers. Applies to `client-v2/` only; v1 unchanged. |
| pV2-01b | Done | Shell chrome — transparent header + avatar widget + user dropdown + footer version chip + login page with dev picker. Builds on shipped pV2-01. New components: `<app-user-avatar>` (initials with theme-soft gradient, configurable size — used in header, login picker, future team list rows, etc.), `<app-user-menu>` (avatar + PrimeNG dropdown with current user / "Switch user (dev)" submenu / Sign out), `<app-shell>` (transparent header + router-outlet + footer chip), `<app-version-chip>` (fixed bottom-right). Stub `AuthService` holds 4 fake users (Sarah/Beth/Ryan/Alex covering all 5 role variants) — same public surface as the real one in pV2-02 so swap is seamless. `/login` shows Google button (stubbed warn) + dev picker section side-by-side. Header is 40px avatar, transparent bg, hairline bottom border. Footer chip `[Dev v2] v2.01a`. Out of scope: real OAuth, real session, org switcher, page hero band. After pV2-02 ships, only the AuthService implementation swaps. |
| pV2-03 | Draft | Team Management (`/settings/team`). Org admins list/invite/role/suspend/remove members of their own org. Row layout per Liam's spec: avatar + name + job title + email, plus three controls — Admin toggle, Suspend toggle, Trash. Invite modal: email (required), name + job title (optional), Admin checkbox. Last-admin guard, self-modification guard, cross-org guard server-side. Pending invitees stub a `users` row with no `google_sub`; on first sign-in via email match, row gets linked + status flips from invited → active. PrimeNG `<p-toggleswitch>` for flags, confirm dialog before trash, 5-second undo toast. No email sending yet (admin tells invitees manually for v1). API endpoints: GET /api/team, POST /api/team/invite, PATCH /api/team/:userId, PATCH /api/team/:userId/status, DELETE /api/team/:userId — all gated by JWT + `can(orgType, isAdmin, 'org.invite_member')`. Out of scope: cross-org Ballpark admin view, email sending, audit log UI, bulk ops, per-member permission overrides. Version chip `[Dev v2] v2.02a`. Sequenced after pV2-02. |
| pV2-02 | Done | Google OAuth + users + roles in `client-v2/`. Real authentication wired in: `passport-google-oauth20` on server, JWT HTTP-only cookie, new `users` + `user_orgs` tables (mirrored to all three schemas via `migrate-schemas.js`), permissions matrix in both client + server, signal-based `AuthService` driving session state, route guards, login + auth-callback pages replace pV2-01 placeholders. Hello page updates to show logged-in user. Self-signup → agency_admin of new org. Old `client-angular/` UNTOUCHED — its dev persona switcher still works. Google OAuth client setup documented in README. Out of scope: Magic Link, invite flow, onboarding flow, org switcher UI, Settings → Team, v1 dev-user migration. Version chip `[Dev v2] v2.01a`. Read auth-and-users-plan.md before implementing. |
| pV2-03 | Done | Team Management (Settings → Team). Admin-gated /settings/team: list members, invite (modal, typed form, creates stub user + status=invited membership), Admin + Suspend toggles, remove (soft-delete with re-invite path). Server /api/team/* with JWT + LIVE membership check per request (suspension bites on the next call), self-mod / last-admin / cross-org guards, writes scoped to the JWT org. pV2-02 upsert extended: first Google sign-in links the stub (google_sub) and flips invited→active + joined_at + default_org_id. Email sending deferred (Resend later); undo toast optional-skipped. Chip v2.05a. |
| pV2-01 | Done | Scaffold `client-v2/` on Angular 21 + PrimeNG 21. Fresh Angular workspace in same repo alongside existing `client-angular/`. Latest Angular (v22), PrimeNG 21 with Aura preset (styled-mode design tokens, NOT the v17 classic theme files), Tailwind, Lucide via `.pick({})`. Standalone-only, OnPush mandatory, strict TS, signals + `@if`/`@for`/`@switch` control-flow, `inject()` over constructor injection. **Runtime-configurable API URL** via `/runtime-config.json` loaded at bootstrap (so customers can change API endpoint post-build without rebuilding — sets up self-host story even though we're not implementing self-host yet). One `--theme-*` token bridge file maps Ballpark brand into PrimeNG Aura tokens — replaces the old app's 160 `.p-*` overrides with ONE place. Runs on port 4201 alongside the v18 app on 4200. Hello-world page calls existing API to prove wiring. Login + auth-callback placeholders for pV2-02. Old `client-angular/` UNTOUCHED. Version chip `[Dev v2] v2.00a`. Out of scope: auth, user state, feature porting, deployment scripts. After this: pV2-02 (Google OAuth + user management). |
| p0038 | Superseded | Inbox-v2 shell rewrite — strangler-fig approach replaced by fresh-instance rebuild. Inbox is now last in the rebuild order (after user management → agent dashboard → marketplace → inbox). See `prompts/pV2-01-scaffold-client-v2-prompt.md` and forward. |
| p0038A | Done | Backup + Angular major-version upgrade. Pre-work for the inbox-v2 arc (orientation doc at `prompts/inbox-v2-plan.md`). CC backs up first (tag `pre-angular-upgrade-v1.69e` + push, create branch `upgrade/angular-v20`), then runs `ng update --dry-run` to audit, checks PrimeNG / lucide-angular / RxJS compatibility, searches for code patterns that break in newer Angular versions. **Hard checkpoint after audit:** CC stops and reports findings to Liam — recommended target version, expected file changes, risks. Liam approves before execution. After approval, CC runs the real `ng update`, bumps deps, applies manual edits, builds clean, smoke-tests key routes (/ /projects /projects/:id /inbox /inbox-v2 /marketplace /settings), bumps version chip to v1.70a, pushes branch (NOT merged to dev yet) so Liam can QC locally. After QC pass: merge to dev. Out of scope: refactoring v1 components to new control-flow blocks, any inbox-v2 work, schema changes. Sets up the `app/v2/` namespace standard for the build arc that follows. |
| p0037 | Done | inbox-v2 shared shell (hero only). Stand up the new inbox-v2 as a **shared, reusable** component with just the hero rendering — no search, no filter drawer, no body. Mirrors v1's pattern: `shared/components/messages-inbox-v2/messages-inbox-v2.component.ts` is the real component; `features/messages/inbox-v2.component.ts` becomes a thin route wrapper that mounts `<app-messages-inbox-v2 viewerRole="agency"/>`. Declares the full reusable contract via Inputs (scope: `scopedToProjectId` / `scopedToSupplierId` / `scopedToItemId` / `viewerRole`; chrome: `showHero` / `showSearchRow` / `showFilterDrawer` / `showTreeRail` / `compact`; events: `threadSelected` / `messageSent`) — flags don't gate anything yet, but the API surface is locked in so future embeds (project page, item card, etc.) compile against the final shape. Route data sets hero defaults (`Inbox` / `Project conversations.` / `back: /home` / `heroAlign: left`); page-settings overlay via HeroSettingsService. Acceptance: `/inbox-v2` loads, hero renders + cog opens settings, below the hero is empty padded space, no console errors, selector mountable elsewhere. Out of scope: any chrome below hero, any data loading, any styles beyond the padding wrapper. Living spec: `client-angular/src/app/features/messages/inbox-v2.schematic.yaml`. After this lands: search-row layer in a follow-up prompt. |

Statuses: **Draft** (still being written) · **Ready** (ready to implement) · **Done** (implemented) · **Mostly Done** (shipped with some sections deferred) · **Superseded** (don't implement).

## Pulling from the backlog

This file is the single source of truth for the prompt set across sessions. Pick from it as needed — there's no longer a strict "lowest-numbered not Done" ordering since the active queue is being driven by feedback and customer-facing priorities, not a sequential burndown. When picking something up: read the `pNNNN-<slug>-prompt.md` and open the matching `pNNNN-<slug>-mockup.html` (if there is one), implement, mark `Done`. When adding new work: take the next `pNNNN`, drop the prompt + optional mockup, add a row to the table above.

